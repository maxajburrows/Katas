package EvenOddTree;

public class EvenOddTree {
    public boolean isEvenOddTree(TreeNode root) {
        // Breadth first traversal
        // Count the level (or a switch) - make sure conditions are met
        return false;
    }
}
